<Window x:Class="DataSortingGame.Views.CardsEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage cards" Width="1080" Height="680" MinWidth="760" MinHeight="420"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource BgBrush}" FontFamily="Segoe UI">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Manage cards" FontSize="26" FontWeight="Bold" Margin="0,0,0,12"/>

        <DataGrid x:Name="Grid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" SelectionMode="Extended"
                  Background="White" Foreground="#0F172A" RowBackground="White" AlternatingRowBackground="#F1F5F9"
                  HorizontalGridLinesBrush="#E2E8F0" BorderThickness="0" FontSize="13.5" RowHeight="30"
                  ColumnHeaderHeight="32" AutomationProperties.Name="Cards">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#E2E8F0"/>
                    <Setter Property="Foreground" Value="#0F172A"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="8,0"/>
                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                    <Setter Property="BorderBrush" Value="#CBD5E1"/>
                    <Setter Property="ContentTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Foreground="#0F172A" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="8,0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#BAE6FD"/>
                            <Setter Property="Foreground" Value="#0F172A"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id, UpdateSourceTrigger=PropertyChanged}" Width="130"/>
                <DataGridTextColumn Header="Label" Binding="{Binding Label, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                <DataGridTextColumn Header="Example" Binding="{Binding Example, UpdateSourceTrigger=PropertyChanged}" Width="220"/>
                <DataGridComboBoxColumn x:Name="CategoryColumn" Header="Category"
                                         SelectedValueBinding="{Binding CategoryId, UpdateSourceTrigger=PropertyChanged}"
                                         SelectedValuePath="Id" DisplayMemberPath="Name" Width="130"/>
                <DataGridComboBoxColumn x:Name="DifficultyColumn" Header="Difficulty"
                                         SelectedItemBinding="{Binding Difficulty, UpdateSourceTrigger=PropertyChanged}" Width="90"/>
                <DataGridTextColumn Header="Explanation" Binding="{Binding Explanation, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <WrapPanel Grid.Row="2" Margin="0,14,0,0">
            <Button Content="Add card" Style="{StaticResource PrimaryButton}" FontSize="15" Padding="18,8" Margin="4" Click="OnAddCard"/>
            <Button Content="Delete selected" FontSize="15" Padding="16,8" Margin="4" Click="OnDeleteSelected"/>
            <Button Content="Save" Style="{StaticResource PrimaryButton}" FontSize="15" Padding="18,8" Margin="4" Click="OnSave"/>
            <Button Content="Cancel" FontSize="15" Padding="16,8" Margin="4" Click="OnCancel"/>
        </WrapPanel>
    </Grid>
</Window>
